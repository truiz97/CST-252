document.addEventListener("DOMContentLoaded", () => {

let showPanel = (e) => {
  console.log (e);
}
let p = document.body.querySelector('#coursename')
console.log(coursename);

});

/**
What element triggers the event? (for example a button, a box you hover over)
- Hovering over the CST 252 tab
- Hovering over the CST 351 tab
- Hovering over the 'x' button
What is the event? ("click," "mouseover," "mouseout", etc.)
- mousover/click tabs & button
What should happen when the event happens? (something opens/closes, something changes style)
-
**/
